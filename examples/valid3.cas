# this is a comment
def test_while (a:Int):Int {
  aux:Int = a;
  res:Int = 0;
  while aux > 0{
    res = res + 1;
    aux = aux - 1;
  }
  return res;
}


def main():Void{
  result:Int = test_while(10);
  print(result);
  return;
}