#bad if/while conditions
def func():Int{

    #if 1+1{ #<--not a bool
    #    return 2;
    #}

    #while 1+1{ #<--not a bool
    #   return 2;
    #}

    #a:Boolean = 1 && true; #&& expects booleans on both sides
    #a:Int = 1 + 1.2; # both sides o + must be of same type and here is INT and FLOAT
    #a:Int = 1.2%1.3; #% requires Ints

    #a:Boolean = 1 == true; #Not same type on both sides
    return 1;
}